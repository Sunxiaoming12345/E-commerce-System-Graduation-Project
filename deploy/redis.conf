# Redis 持久化配置
# RDB + AOF 双写，崩溃后最多丢 1 秒数据

# === RDB 快照（保留默认） ===
save 900 1
save 300 10
save 60 10000

# === AOF 增量日志 ===
appendonly yes
appendfsync everysec
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

# === 其他 ===
dir /data
databases 16
